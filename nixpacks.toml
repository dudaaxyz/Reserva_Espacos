[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader", "npm install", "npm run build"]

[start]
cmd = "php artisan config:clear && php artisan migrate --force && php artisan storage:link && php-fpm & nginx -g 'daemon off;'"